{
  "name": "perpetua-browser-extension",
  "private": true,
  "scripts": {
    "test": "vitest run --globals --environment jsdom --maxWorkers=1"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "vitest": "^3.2.4"
  }
}
